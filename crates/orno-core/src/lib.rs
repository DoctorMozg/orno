//! `orno-core` — library surface for the orno orchestrator.
//!
//! The skeleton is intentionally empty. Phase 2 introduces the module tree
//! and trait seams (`NodeExecutor`, `LlmTransport`, `EventSink`).
